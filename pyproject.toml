[project]
name = "nexus-ai-empire"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.0",
    "stripe>=9.0.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "webhook_stripe:app"
